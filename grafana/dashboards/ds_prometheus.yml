# https://github.com/ruanbekker/docker-prometheus-grafana/blob/master/grafana/provisioning/dashboards/ds_prometheus.yml
apiVersion: 1

providers:
  - name: 'Prometheus'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards
